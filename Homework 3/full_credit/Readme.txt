To compile and   run the main.cpp file enter the directory full_credit and type the command make
Then type ./a.out

To compile and run the test.cpp file enter the full_credit directory and type the command make test
Then type ./a.out

After compiling and running type make clean to remove all executables 